Installation:

    git clone --recurse-submodules -j8 git@github.com:ReaperUnreal/Vim-Setup.git ~/vim

Create symlinks:

    ln -s ~/vim ~/.vim
    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc
